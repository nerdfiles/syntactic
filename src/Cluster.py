from sets import Set

from syntax.learner import Vocabulary
from syntax.learner import Word


class Cluster:
    self.id = null
    self.totalSize = 0
    self.wordCount = 0
    self.words = Set(10) words
    self.distribution = Null
    self.vocab = Vocabulary
    self.learner = Learner
    self.isNew = false

    def Cluster:

    def clusterDistribution(self):
        if learner.isClusterUpdated(self):
            return distribution

    def add(self, cluster):

    def add(self, int item):

    def remove(self, int item):

    def reset(self):

    def getID(self):

    def isNew(self):

    def setNew(self, bool b)
